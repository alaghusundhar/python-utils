def reverse(text):
    a = ""
    print(len(text))
    for i in range(1,len(text)+1):
        a += text[len(text) - i]
        print(a)
    return a

def reverseword(text):
    str=""
    for i in len(text):
        a +=text[len(text)]

reverse("Hello")